# GAN 主实验 —— 修改记录（R1–R9）

> 依据：对两轮 DGM-H 运行（`generate_20260915_194845_690053` 10 代有提升、
> `generate_20260916_234111_094614` 20 代零提升）的日志/产物分析。
> 约束：**评分标准不变**（`domains/report.py` 判定语义不改）；不刻意拆分 DGM-H/GAN 代码，DGM-H baseline 由外部维护。
> 术语：**冻结** = 默认不可被进化修改；**可解冻** = 记为候选，未来作为独立研究轴。

---

## R1 反馈接地与评估点

**问题**
- evaluator 只收到 `run_summary={genid, score, meta}`，`meta` 只有 `report_path` **没有 report 内容**；
- paper_review 只认小写精确 `accept|reject`，而 seed task agent 输出整篇综述 → 客观分恒 0；
- 评估点是 opt-in 且**初始 `eval_points` 为空** → evaluator 默认没有任何检查项，无法发现"输出不符合契约"。

**原设计**
- `run_summary` 无评测事实；`contract/output_contract` 不存在；评估点默认全关。

**新设计**
- 契约是**框架客观计算**，**不作为评估点**；计算结果作为事实注入 `run_summary`。
- 判断类评估点（trajectory_quality / hard_failure / reward_hacking / rule_violation）**可选但初始默认选中**，保留自进化空间。
- 不设 always-on 评估点（always-on 仅用于工具）。

**具体改动**
- `gan/framework/domains.yaml`：每域新增 `output_contract`（paper_review=`accept|reject`；imo_grading 等；default=freeform）。
- `gan/framework/task_execution.py`（新）：`compute_report_summary()` → `label_distribution / invalid_rate / coverage / contract_ok / sample_n`。
- `gan/framework/loop.py`：`evaluator.evaluate(run_summary=...)` 注入 `report_summary`。
- `gan/build.py`：`_seed_self_designs()` 默认把评论类 `eval_points` 写入 evaluator 初始 design。

---

## R2 / R3 substrate 冻结、容错与重试

**问题**
- substrate/工具序列化缺陷会被 agent 当成"待修问题"，把进化方向带偏（20 代 run 反复改 `llm_withtools.py`/模型名并自伤出 503/model_not_found）；
- 429/503 在应用层**不重试**（backoff 异常元组不含 litellm 异常）→ 一题失败即整批 exit 1 → 整代 N/A；
- `resume` 把空预测当"已完成"，不会重试；无 fallback 模型；
- 冻结文件被拒时的 `DENIED (frozen substrate...)` 会被回传给 agent，反而引导它去动信任锚。

**原设计**
- `agent/llm.py` backoff 仅 `requests/JSONDecode/KeyError`（本地另加 `RateLimitError`）；
- `domains/harness.py: run_agent` 无 try/except；resume 用 `~isna()` 判完成；
- `request_source_access` 回传 `denied` 明细；`deny_paths` 硬编码在 `loop.yaml`。

**新设计**
- substrate = **冻结但可解冻**；拒绝告警**不外泄**（只审计）；
- 重试覆盖 rate-limit/5xx/timeout + `GAN_MODEL_FALLBACK` 兜底； **[已变]** `GAN_MODEL_FALLBACK` 已废除（v2 起模型统一由 `gan/framework/models.yaml` 管理：无 env、无 fallback、显式传参）。
- harness **逐题隔离** + resume 修正；冻结清单**单一事实源**。

**具体改动**
- `gan/framework/frozen.py`（新）：`FRAMEWORK_GLOB` + `EXTERNAL_FROZEN` + `deny_paths()`（单一事实源）。
- `gan/framework/loop.yaml`：移除硬编码 `deny_paths`；`gan/build.py` 改从 `frozen.py` 取。
- `gan/tools/deep/request_source_access.py`：不再回传 `denied`（仅 `events.jsonl`/`broker.last_result`）。
- `gan/tools/design/set_param.py`（拒绝 `model`）、`set_config.py`（剥离 `params.model`）：模型选择冻结。
- `agent/llm.py`：backoff 扩到 `RateLimitError/ServiceUnavailableError/Timeout/APIConnectionError`（+ openai SDK 错误）；~~`GAN_MODEL_FALLBACK`~~
  （**已废除**：fallback 模型随 v2「模型配置统一管理」一并删除，最终策略 = 无 env、无 fallback、显式传参，见下文附录）。
- `domains/harness.py`：逐题 `try/except`（失败写 `evals/eval_failures.jsonl`、留空预测）；resume 空预测重试。
- `scripts/local/test_frozen.py`（新）：冻结布局不变式。

---

## R4 随机基线

**问题**
- 裸用原始分会把"从坏到瞎猜"当提升（paper_review random≈0.52）；
- 多域 `random_guess` 不同，`analysis` 直接平均原始分不可比；
- `report.py` 的 rg 在**过滤掉空预测后的 df** 上算，分母会漂移。

**原设计**
- `RewardPacket.numeric.benchmark` = 裸分；`analysis/plot_progress.py` 直接平均原始分。

**新设计（仅记录，未实施）**
- 有效分数 `excess = max(0,(acc−rg)/(1−rg))`；`rg` 在**固定请求样本集**上算；
- 图上额外标注随机基线；多域聚合用 excess。

**具体改动**
- 未实施。待办：`gan/framework/reward/packet.py` 增 `random_guess`/`excess`；选择与 `analysis/*` 改用 excess。

---

## R5 未评分、失败轮与版本管理

**问题**
- 全 0 并列时选择无梯度；失败代与真 0 分被混同；
- 评测崩溃（harness exit≠0）直接整代 N/A；
- planner/evaluator 是**单一可变实例**，`self_improve` 原地覆盖、无 archive、无回退。

**原设计**
- `task_runner` 失败返回 `None` → `inner_skip`（不入档）；
- `NodeValue` 仅 `score`；外循环无 checkpoint；角色树只记事件、不参与选择。

**新设计**
- 未评分分四类：pending / failed / partial / invalid；
- **失败轮 invalid**：记档但 `valid_parent=False`；bench 错误也传给 evaluator；
- **分数回退**：partial=有效均值+`coverage`；完全缺失=**同父兄弟中位数，不足 2 个取父代分**；imputed **不作 evaluator 锚点**；
- **外循环 checkpoint**：落盘/回退/接续（含角色设计快照 + RNG seed），不含父代选择； **[已变]** v5 已删除 RNG seed：checkpoint 的 `state` 不再含 `seed`（见 `gan/framework/checkpoint.py`）。
- **角色设计接纳**：外循环无实测提升则回退角色 self-improve；
- 保留现选择算法（`gan/framework/tree/store.py` UCB）；**任务父代选择策略=冻结且可解冻**。

**具体改动**
- `gan/framework/tree/store.py`：`NodeValue` 增 `score_status`(`ok|partial|imputed|invalid`)/`coverage`。
- `gan/framework/task_runner.py`：产出 `score_status/coverage/report_summary/invalid_reason`。
- `gan/framework/checkpoint.py`（新）：`save/load/restore`（state + trees + designs）。
- `gan/framework/loop.py`：invalid 记档、imputation、`report_summary` 透传、每内循环/外循环 checkpoint、`resume`、`rollback_to_outer()`、角色设计快照+非提升回退；仅实测(ok/partial)计 improved。
- `gan/framework/task_execution.py`：`extract_score()`（NaN→None）、`compute_report_summary()`。

---

## R6 / R7 冻结边界、组件开放度与归属

**问题**
- "冻结"有两个来源（物理位置 + `deny_paths` 名单），已出现悬空引用（移动一半）；
- 组件与测量边界模糊；reward/task_runner 的归属未定；
- 设计落盘/胶水层散落在 `task_runner`。

**原设计**
- 冻结物散落 `gan/` 顶层（`loop.py/context.py/access.py/tree/reward/task_runner.py`）；deny 硬编码。

**新设计**
- **冻结 = `gan/framework/*`（结构性不变量）+ 显式 external**（`agent/llm*.py, agent/base_agent.py, domains/{harness,report}.py`）；external 不移动（避免依赖反转）；
- 组件放宽：`gan/components/`、`gan/registries/`、`gan/tools/` 在深改门控下可新增/编辑；
- `reward` 与 `task_runner` **全部冻结**；design 落盘 + 胶水层并入 `gan/framework/`；
- 目前**不存在**"改变评分口径"的组件 → `affects_scoring` 审批**暂缓**；
- 主实验冻结但**可解冻候选**：`loop.py`、`tree/*`。

**具体改动**
- **布局迁移**：`git mv` `gan/loop.py→gan/framework/loop.py`、`gan/context.py→gan/framework/context.py`、
  `gan/access.py→gan/framework/access.py`、`gan/tree/→gan/framework/tree/`（连同此前 `task_runner.py`、`reward/`）。
- `gan/framework/task_execution.py`（新）：design 落盘 + env/技能装配 + 仓库/patch + harness/report + `report_summary`。
- `gan/framework/frozen.py`（新）+ `gan/build.py`（deny 来自 frozen）+ `scripts/local/test_frozen.py`。
- 文档：`AGENTS.md`、`docs/GAN实现记录.md`（迁移 banner）。

---

## R8 逐条 issue 反馈链路

**问题**
- `judge_fix` **未调用**时 `fixed=False` → 被记成 `accepted_unfixed`（"已接受但未修复"），是对 planner 的**不实指控**，且污染 evaluator 自反馈与 2×2 统计。

**原设计**
- `classify_issue`: `fixed = bool(verdict.fixed) if verdict else False`；2×2 只有 4 格；digest 无"未判定"。

**新设计**
- 引入三态 `fixed ∈ {true,false,unjudged}`；`unjudged` 单列，2×2 只统计**已判定** issue；
- 历史反馈=**整体一条**（无"最近 N 条"上限）；不做框架级"未结项账本"，未结项由 evaluator 自然发现（并入当轮 issue 或升级为评估点）；`self._last_feedback` 单槽足够；遗忘用诊断（`events.jsonl`）而非强制；
- 两轮延迟保留（记录）。

**具体改动**
- `gan/framework/reward/evaluator_reward.py`：新增 `CELL_UNJUDGED`、`IssueOutcome.judged`、`classify_issue` 三态、`_LABEL_TEXT`/digest 单列（"not judged"）。

---

## R9 对照消融归属

**问题**
- 若让系统参与决定"比什么、怎么算"，进化可优化掉对自己不利的对照，结论不可比。

**原设计**
- 无成文归属。

**新设计（仅记录，不改代码）**
- **对外实验协议 + 插桩 = 冻结框架/外部**；**evaluator 内部判据（评估点/阈值）= 自进化**；二者不混在同一可编辑面。

**具体改动**
- 无代码改动；`scripts/local/test_frozen.py` 属框架插桩。

---

## 附：模型配置统一管理（已实施，v2：三部分 + 纯查表）

**问题**
- 模型配置散落 4+ 处（`agent/llm.py` 常量 / `loop.yaml` / `build` / `loop.params` / `task_runner` / domain utils / 多处 env），存在优先级语义重复、漂移、不可追踪；
- 模型名混进**可进化** design 的 `params.model`；
- `scripts/dgmh`、`domains/*/utils.py`、balrog/genesis 工厂各写各的默认。

**原设计（v1）**
- 优先级链 `explicit > env > domain > models[key] > models.task` + `fallback`；
- 域 task 默认放在 `models.yaml: domains.<domain>`。

**新设计（v2，最终）**
- `gan/framework/models.yaml` 分三部分，**唯一解释**：
  - `gan.{task,planner,evaluator}`；
  - `dgmh.{meta,task}`；
  - `domains.<role>`（仅**非 task** 的域专属角色）。
- `gan/framework/models.py` = **纯查表**（`resolve`/`resolve_section`/`describe`），**无 env、无 fallback、无优先级链**。
- 所有 **domain 的 task agent 共用驱动的 task 配置**，由驱动方解析后 **运行时显式传递**（不再是域属性、不再读 env）。
- 角色归并：**proof grader、balrog 策略 agent、genesis reward 合成 → 全部并入 task agent**；polyglot 的 aider CLI 为**独立**域角色（`domains.polyglot_aider`）。

**角色盘点（归并后）**

| 角色 key | 覆盖的 agent | 运行方式 |
|---|---|---|
| `gan.task` | GAN 的 task agent（paper_review/search_arena/imo/polyglot/balrog/genesis 的通用 task） | 驱动解析 → `--model` 显式传给 `domains.harness` |
| `gan.planner` / `gan.evaluator` | GAN 两角色 | GAN build 直接构造 |
| `dgmh.task` | DGM-H 的 task agent（含 polyglot run_task_agent） | 显式传参 |
| `dgmh.meta` | DGM-H meta agent（含原 polyglot meta、DGM baseline coding/diagnose） | 显式传参 |
| `domains.polyglot_aider` | polyglot benchmark 的 aider CLI（外部工具） | CLI 默认值 |

**具体改动**
- `gan/framework/models.yaml`：三部分结构；删除 `fallback`、`domains.<domain>` 的 task 默认。
- `gan/framework/models.py`：重写为纯查表（删除 env 别名 / fallback / 优先级）。
- `gan/build.py`：`resolve("gan.task"/"gan.planner"/"gan.evaluator")`；**删除 explicit 覆盖参数**（`task_model/…`）；写 `model_config` 事件。
- `gan/framework/{task_execution,task_runner}.py`：**不再设 `GAN_TASK_MODEL`**；改为把模型作为 `--model` 传给 `domains.harness`。
- `domains/harness.py`：新增 **必需的 `--model`**；`harness(..., model=)` 无 model 直接报错；删除 env/legacy 解析。
- `domains/report.py`：`report_imo_proof(..., model)` 与 `--model`（proof grader 现为 task 角色，仍显式传模型）。
- `domains/polyglot/harness.py`：`harness(..., model=)` → `process_entry` → `run_task_agent --model`。
- `scripts/dgmh/{run_meta_agent,run_task_agent}.py`：`resolve("dgmh.meta")` / `resolve("dgmh.task")`。
- `scripts/dgmh/generate_loop.py`：eval/report 传 `--model resolve("dgmh.task")`；meta 传 `resolve("dgmh.meta")`；启动写 `[model_config]` 日志。
- `scripts/run_gan.py`：删除 `--task/planner/evaluator-model`（不再有显式覆盖）。
- `domains/balrog/agents`、`domains/genesis/agents`：`AgentFactory` 改用 `config.model`；两份 `config.yaml` 增顶层 `model: null`，由 `domains/harness.py` 注入 `model=<id>`。
- `domains/polyglot/benchmark.py`：aider CLI `--model` 默认取 `resolve("domains.polyglot_aider")`。
- **`agent/llm.py` 清理**：删除全部 `*_MODEL` 常量、`GAN_MODEL_DEFAULT` 覆盖与 fallback 逻辑；`get_response_from_llm(msg, model, ...)` 的 `model` 变为**必填**，仅保留重试（不换模型）。`agent/base_agent.py`、`agent/llm_withtools.py` 同步去掉默认模型；`baselines/dgm/{utils,coding_agent}.py` 改用 `resolve("dgmh.meta")`。

**运行时的模型配置显式记录**
- GAN：`gan/build.py` 每次 build 写 `events.jsonl` 的 `model_config` 事件；每个 `Node.meta["model"]` 记录当次使用的模型。
- DGM-H：`generate_loop()` 启动写 `[model_config] {...}` 到 `generate_loop.log`。
- 域：`domains/harness.py` 收到的 `--model` 即最终值（显式传参，可追踪）。

**待定**
- `preflight()`（启动前模型可用性探测）尚未接入；
- 多域运行统一用单一 task 模型（已定）；若未来某域确需不同 task 模型，需在驱动方显式增加，而不是回到域/环境配置。

**关于 `scripts/local/test_frozen.py`**
- 冻结布局检查已从 `scripts/tests/` 移到 **`scripts/local/`（gitignored）**，作为**本地一次性检查**，不作为需要持续维护的仓库测试。

---

# v4 改动（一：实例化 / 工具面 / 单链 / per-outer ckpt）

> 依据：对"eval_points 改动后 tools_dir 不同步""新 Evaluator 的 acceptance 用旧结果"
> 两个问题的审查，以及后续关于"实例、workspace、chat history、ckpt、trajectory、
> 深度编辑、operators"的设计讨论。M1 只落地其中不依赖新子系统（trajectory 归档、
> 深度编辑、沙箱）的部分。

## V4.1 术语：实例（instance）

**问题**：此前没有统一的"实例"概念：planner/evaluator 是全程常驻对象，task agent 是
per-question 子进程；设计/工作区/chat 的创建、继承、变更、清理时机各自为政。

**新设计**：**实例 = 角色 + 工作区 + 所属 chat history**。
- task 实例：每 inner（一个 `genid`）刷新；
- planner/evaluator 实例：每 outer 刷新。

**具体修改**
- `gan/roles/base_role.py`：`Role` 新增 `instance`（如 `outer_3`）；新增
  `refresh_tools()` / `tools_dir_for()` / `default_chat_path()`。每个实例的工具目录为
  `output_dir/toolsets/<role>/<instance>/`，chat 为 `output_dir/chat/<role>/<instance>.md`。
- `gan/roles/{planner,evaluator}.py`：`__init__` 接受 `instance`。
- `gan/framework/loop.py`：新增 `_refresh_roles(outer)`；每个 outer 开始调用，重建
  planner/evaluator 实例并清空其源码工作区。`GanLoop` 支持 `planner_factory`/
  `evaluator_factory`（不传则复用注入实例，兼容离线/冒烟）。
- `gan/build.py`：改为传工厂，实例键 `outer_<N>`。

## V4.2 tools_dir 陈旧（原"问题一"）

**问题**：`Role.__init__` 只在构造时 `assemble_tools_dir` 一次；self_improve 改
`eval_points` 后工具目录不重装；且装配只增不删。

**新设计**：实例刷新时**重建**工具目录；装配前清空（保留 always-on 由重拷贝保证）。

**具体修改**
- `gan/tools/assembly.py`：`assemble_tools_dir(..., clear=False)`；新增 `_clear_tools_dir`
  （仅删本目录顶层 `*.py` 与 `__pycache__`）；删除未使用的 `load_operators`。
- `gan/roles/base_role.py`：`refresh_tools()` 以 `clear=True` 装配。
- 效果：新增 eval_point/skill 生效；**移除的组件真正从工具目录消失**。

## V4.3 工具增删改（浅层/深层）与移除 mint_operator

**问题**：`mint_operator` 只写 `config["operators"]` 且**无任何消费者**，是空操作；
schema 的 `operators` 槽无意义。

**新设计**（按"触及什么"分类，不预设"声明式组合"）：
- **浅层增/删**：只改实例 design 配置列表（`skills`/`eval_points`/`memory`），**不动注册表与源文件**。
- **深层删**：改注册表 + 删源码文件（算子封装，走 gate/审计）。
- **深层增/改**：新组件/新逻辑 → **必须触及源码**。

**具体修改**
- **删除** `gan/tools/design/mint_operator.py`（`git rm`）。
- 新增 `gan/tools/design/deselect_component.py`（浅层删）。
- `gan/design/schema.py`：从三角色的 schema/default 移除 `operators`。
- `gan/roles/{planner,evaluator}.py`、`gan/design/seeds/planner.md`：更新算子说明为
  `select_component`/`deselect_component`/`set_*` + 深层 `request_source_access`。
- 深层 op 一致性：`planner.py` 生成 patch、`task_runner._modify_depth` 现识别
  `request_source_access`（`intent=modify`）与旧的 `code_edit`。

## V4.4 单链 p/e（去除 accept/reject，原"问题二"）

**问题**：`outer_improved` 用**旧** P0/E0 期间的任务结果，决定**新** P1/E1 的去留；
候选从未被评估。

**新设计**：**单链**——每个 outer 结束保存 ckpt；下个 outer 从上一代 ckpt 的设计启动；
**无 accept/reject、无父代选择**。`improved` 仅记录用于分析。

**具体修改**
- `gan/framework/loop.py`：删除 `designs_before` 快照与 `restore_designs` 回滚；
  新增 `self_improve_kept` 事件。

## V4.5 ckpt：per-outer 不可变 + 日志不被截断

**问题**：`checkpoints/outer.json` 固定名覆写，无 per-outer 历史；`restore_trees`
用 `"w"` 重写 append-only 的 `*_tree.jsonl`，回滚会丢历史。

**新设计**：`checkpoint.json` 为 latest（覆写）；outer 边界另写**不可变编号文件**
`checkpoints/outer_<N>.json`；恢复改为**追加 `op=reset` 事件**，不截断日志。

**具体修改**
- `gan/framework/checkpoint.py`：`save_checkpoint(..., index=)`；新增
  `latest_outer_index()`/`list_outer_checkpoints()`；原子写（tmp + `os.replace`）；
  `load_checkpoint(...,"outer")` 读最新编号（兼容旧的 `outer.json`）；
  `restore_trees` 改为追加 `reset` 事件。
- `gan/framework/tree/store.py`：`_apply` 支持 `op=reset`（内存重建节点集）。
- `gan/framework/loop.py`：`_save_checkpoint` 传 `index=outer`。

## V4.6 trajectory 可见范围（配置，管线待实现）

**决定（v0）**：p/e 查询 task 跨轮 trajectory 时，**仅允许直接父代**；最多可追溯的
inner 代默认等于 `loop.inner_max`。默认投递仍只当前代，历史走工具主动查询（管线待 M2）。

**具体修改**
- `gan/framework/loop.yaml`：新增
  ```yaml
  trajectory:
    parent_only: true
    max_inner_generations: 3   # default = loop.inner_max
  ```

## V4.7 运行/日志新增

- 每 outer：`events.jsonl` 新增 `role_refresh`、`self_improve_kept`。
- per-outer ckpt：`checkpoints/outer_<N>.json` + `checkpoint.json(latest)`。

---

# v4 改动（二：持久化 / trajectory / 访问审计 / 深度编辑）

## V4.8 trajectory 归档 + 脱敏 + 受控读取
- 新增 `gan/framework/trajectory.py`（**冻结**：脱敏属于信任锚，不可被进化削弱）：
  `collect(run_dir, run_id, output_dir, genid)` 从
  `<run_dir>/outputs/<run_id>/agent_evals/chat_history_*.md` 归档**脱敏副本**到
  `output_dir/trajectories/<genid>/`，并追加 `trajectories/index.jsonl`
  （qid/file/bytes/sha256/redacted/ts）。
- `redact()`：按敏感键（`overall_accuracy`/`random_guess_accuracy`/
  `label_distribution`/`report_summary`/`"score"`/`benchmark_score`）整行替换为
  `[REDACTED:sensitive]`。
- `visible_genids(tree, genid, parent_only, max_inner)`：**v0 仅直接父代**； **[已变]** 该函数已删除（v4.20）：可见性改由 loop 显式传入 `AccessContext.trajectory_genids`。
  `parent_only=false` 时按 `max_inner` 上溯祖先链。
- `read(output_dir, genid, max_chars)`：返回脱敏截断文本。
- 新增 `gan/tools/work/common/read_trajectory.py`（planner/evaluator 共用的
  always-on 工作工具）：从 `AccessContext(broker,node_id)` 解析 output_dir 与当前
  genid，按 `loop.yaml: trajectory.parent_only`/`max_inner_generations` 校验可见性后
  返回；越权返回错误。
- `gan/tools/assembly.py`：`always_on_dirs` 对 planner/evaluator 追加
  `work/common`（task 仍无 always-on）。
- `gan/framework/task_runner.py`：harness 跑完后调用 `trajectory.collect(...)`。

## V4.9 得分索引（快速查验）
- 新增 `gan/framework/scores.py`：`append_score()` / `read_scores()` /
  `sha256_file()` → `output_dir/scores/scores.jsonl`
  （genid/domain/score/score_status/coverage/report_sha/valid_parent/ts）。
- `task_runner` 记录 `Node.meta["report_sha"]`；`loop` 在节点入库后调用
  `_append_score()`。权威证据仍是 `runs/<genid>/report.json`，索引用于快速查询。

## V4.10 访问审计落 node + 冗余字段清理
- `gan/framework/loop.py`：新增 `_source_access_log(genid)`，把 planner/evaluator
  的 grants 写入 `Node.source_access_log`（不再只停留在 events.jsonl）。
- `gan/framework/tree/store.py`：移除未使用字段 `planner_genid`/`evaluator_genid`/
  `run_full_eval`/`prev_patch_files`；保留 `source_access_log`/`curr_patch_files`。

## V4.11 task 运行隔离（阶段 1，无容器）
- `gan/framework/task_execution.prepare_run_dir`：**无条件**使用 per-gen 仓库副本
  `nodes/<genid>/repo`（取消"无 patch 即 repo_root"分支），任务运行不再污染
  `repo_root`；有 patch 时应用在副本内。
- 未做：task 沙箱内**不可见 dataset/report**（需子进程/容器沙箱，M3）。

## V4.12 验证
- `scripts/local/smoke_p1.py` 新增：`test_toolset_clear`、`test_outer_ckpt_numbering`、
  `test_deep_edit`（授权→受限编辑→diff→patch，越界拒绝）、`test_trajectory`
  （脱敏 + parent-only + 归档读取）、`test_scores_index`；
  `test_toolset_assembly` 增加 `read_trajectory`/`edit_source`；`test_dual_loop` 增加
  `role_refresh`×outer 与 `self_improve_kept`。
- `smoke_gan` / `smoke_p1` / `test_frozen` / 全量 `py_compile` 全绿。

## V4.13 深度编辑（受限，不引入 bash）
- `gan/tools/deep/request_source_access.py`：授权成功时回传工作区 `src` 绝对路径；
  `intent="modify"` 时提示用 `edit_source` 编辑。
- 新增 `gan/tools/work/common/edit_source.py`（p/e always-on）：只暴露基础
  `editor` 能力（view/create/str_replace/insert/undo_edit），**路径强制限制在实例
  工作区 `src/` 内**，越界拒绝。不会给角色 `bash`（无法约束到工作区）。
- 流程：`request_source_access(modify)` → `edit_source` 改工作区副本 →
  `build_patch_from_workspace` diff → patch 应用到下一代 task 运行。
- 未做：把 p 的自改 patch 应用回角色自身源码（需 per-run code 仓 + 编译校验 +
  下一 outer 新进程重导入）。

## v4 剩余待办
- **benchmark 泄漏根因隔离**：task 沙箱中不含 dataset/report；越权读取以
  `unauthorized_access` 证据（不含标签值）交 e 判定。
- **角色自改源码闭环**：per-run code git 仓 + 编译校验 + 把 p 的自改 patch 应用到
  自身源码，并于下一 outer 新进程重导入。
- **workspace 按 `outer_<N>` 命名**（当前为整体删除）。
- **ckpt 三层**：per-run code git 仓（code_commit）、`logs/`、`trajectories/` 的
  manifest 化；`curr_patch_files` 语义修正。
- **operator 深层封装**：`unregister_component`（注册表+源码，走 gate）。
- 文件轮转（trajectory/log 分片）与 `preflight()`。

---

## V4.14 沙箱与 bash：暂缓（决策记录）

**结论**：本轮**不引入沙箱，不向 p/e 开放 bash**。

**分析要点（留档）**
- 纯子进程**不是安全沙箱**：同文件系统/用户，绝对路径与 `..` 可逃逸；只能做"故障隔离"（超时/rlimit/被杀）。
- 要让 bash 可安全开放，需要 OS 级限制：`bwrap`/mount namespace、Landlock、或低权用户+权限；WSL2 的 user namespace 支持需实测。
- 关键简化：LLM 调用在**父进程**，只有**工具执行**需要沙箱 → 可只对 `bash` 做**工具级沙箱**，沙箱内无需网络/API key，不必容器化整个 harness。
- 现阶段优先级：先完成 allowlist（按角色读/写）、grant 路径防护、运行副本允许清单、输出分桶、JSONL 日志；这些是沙箱的**前置**，也将沙箱职责缩小。
- 触发再上沙箱的条件：角色开始执行自写代码 / 需对抗性防泄漏 / 需多依赖域（polyglot 等）/ 需 GPU（balrog/genesis）。

**当前替代措施**
- p/e 不给 `bash`，只用工作区受限工具（`edit_source` + 只读 `read_file`/`list_dir`/`grep`）。
- task 运行副本采用**允许清单**（不含 dataset/report），数据集经 `GAN_DATASET_ROOT` 由 harness 在副本外读取（弱隔离）。 **[已变]** v5/fix1：标签改由父 harness **仅经 CLI `--dataset_root`** 读取；`GAN_DATASET_ROOT` 已从所有子进程 env 中清除（代码中不再有写入点）。
- 未来沙箱后端：优先 `bwrap`（轻量），不可用则容器（复用 DGM-H 模式）。

## V4.15 实现：布局分桶 / 角色化 allowlist / 工作区按 outer / JSONL / 受限读工具 / 运行副本允许清单

**输出布局（单一来源 `gan/framework/paths.py`）**
- `ckpt/`（`checkpoint.json`、`outer_<N>.json`、`design/`）、`logs/`（`events.jsonl`、
  `*_tree.jsonl`、`task_runner.log`）、`trajectory/outer_<O>/{planner,evaluator,<genid>/{task,planner,evaluator}}.jsonl`、
  `scores/scores.jsonl`、`runs/<genid>/`、`work/<genid>/`（临时）、`workspaces/<role>/outer_<O>/`。
- 改动：`gan/framework/{paths,tree/store,checkpoint,access,loop,task_runner,scores,trajectory}.py`、
  `gan/design/store.py`、`gan/roles/base_role.py`、`gan/build.py`。

**访问边界：改为"按角色 allowlist"**
- `gan/framework/frozen.py`：不再维护冻结黑名单，改为角色读/写白名单——
  `task` 无源码权限；`planner` 读+写 t-set∪p-set；`evaluator` 读 t-set（只读）+ 读/写 e-set；
  `TRUST_ANCHOR` 仅作文档/双保险。
- `gan/framework/access.py`：按 `(role,intent)` 白名单判定；新增防递归/防膨胀：
  拒绝 repo 根、目标在源内、源是 workspace 祖先；目录复制忽略 output 子树；单次授权
  `max_files`/`max_bytes` 上限。
- `scripts/local/test_frozen.py`：改为校验角色白名单与 trust anchor 不可授权。

**工作区按 outer + self_improve 可访问源码**
- `Role.access_key()`：工作区键 = 角色实例（`outer_<O>`）；`plan/evaluate/self_improve` 共用。
- `self_improve(..., broker=...)` 现在设置 `AccessContext`，可 `request_source_access`；
  `loop` 调用时传 `broker`。外层开始 `clear_workspace(role)`。

**对话/轨迹：md → 结构化 JSONL（每实例一份）**
- 新增 `utils/trajectory_log.py`（共享写入器）；`agent/base_agent.py` 与
  `agent/llm_withtools.py` 写入 `{kind: input/output/tool_call/tool_output/log,...}`；
  `task_agent.py` 透传 trajectory 文件。
- GAN 角色会话文件：`trajectory/outer_<O>/<genid>/<role>.jsonl`（plan/evaluate）、
  `trajectory/outer_<O>/<role>.jsonl`（self_improve）；task 由 `loop` 调
  `trajectory.collect(...)` 汇总为 `<genid>/task.jsonl` 并**按 JSON 字段脱敏**。
- 同步 DGM-H/polyglot/baselines 的 md→jsonl（写入与**读取方**）：
  `domains/harness.py`、`domains/polyglot/harness.py`、`domains/{balrog,genesis}/evaluator.py`、
  `scripts/dgmh/{generate_loop,run_meta_agent}.py`、`baselines/dgm/utils.py`。
- 退役 `utils/thread_logger.py`。

**task 运行副本：允许清单最小复制**
- `gan/framework/task_execution.prepare_run_dir(..., domain=)`：只复制
  `task_agent.py`、`agent/`、`utils/`、`domains/{__init__,harness,report}.py`、`domains/<domain>/`
  （排除 dataset/重资产）；数据集经 `GAN_DATASET_ROOT` 在副本外读取。 **[已变]** 改为仅经 CLI `--dataset_root` 传入父 harness，`GAN_DATASET_ROOT` 不再下发。

**受限读工具（p/e 无 bash）**
- `gan/framework/workspace.py`（限根解析）+ `gan/tools/work/common/{read_file,list_dir,grep}.py`，
  与 `edit_source` 一样**限定在实例工作区 `src/`**。

**验证**：全量 `py_compile`、`smoke_p1`（含新增工具/路径/白名单断言）、`smoke_gan`、
`test_frozen` 全绿。

## V4.16 实现续：可用性/清理/preflight/自改留痕

- **暴露可编辑边界**：新增 `gan/tools/work/common/list_editable.py`，返回本角色的
  `read/write` 白名单；`request_source_access` 在"提供了路径但全被拒"时改回
  "No accessible paths ... call `list_editable`"（修掉误导性回执）。
- **运行副本清理 + 证据外提**：`task_runner` 跑完把 `report.json`/`predictions.csv`
  复制到 `runs/<genid>/`；`loop` 在 `trajectory.collect` 后删除 `work/<genid>`
  （`loop.yaml: loop.keep_workdirs=false` 可保留）。
- **精炼评测 schema 落盘**：新增 `runs/<genid>/eval.json`（predicted_score/issues/
  fix_verdicts/penalties/summary/weaknesses/suggestions）。
- **`preflight` 接线**：`gan/build.py: preflight=True` 时探测三个 GAN 模型并写事件，
  失败即抛错；`scripts/run_gan.py --preflight`。
- **自改留痕（record-only）**：`planner/evaluator.self_improve` 在发生
  `request_source_access(modify)` 时用 `build_patch_from_workspace` 产出 patch 并返回；
  `loop._save_self_patch` 落盘 `ckpt/self_improve/outer_<N>_<role>.patch` 并记事件。
  （**apply 到角色源码仍待 code 仓/新进程步骤**。）
- **`curr_patch_files`**：不再塞 `design_path`，改为空；设计路径仍在
  `Node.meta["design_path"]`。
- **验证**：离线全绿（compile/smoke_p1/smoke_gan/test_frozen/build）。

## V4 剩余待办（更新）
- ~~角色自改源码闭环（apply）~~ → 已在 **v4.17** 实现（code 仓 + driver/worker + apply/回滚）。
- ~~operator 深层封装（unregister_component）~~ → 已实现（见 v4.17 Phase E）。
- ~~文件轮转~~ → 已实现（trajectory_log 分片 + read_all）。
- **沙箱**：bwrap（工具级，让 bash 可开放）或容器。
- **共享层归位**：`models`/`loader` 上移至 `utils/`（本轮不做）。
- **`e` 是否可见 `t` 源码**：当前实现为"只读可见"，待最终确认。
- **自改 patch 校验**：当前 `py_compile`；可选加"空载 import/smoke"。

---

# v4.17 设计：code 仓与自改 apply 闭环（实施中）

## 目标
- p 深改 **t**（下一代 task 生效）与 **p 自身**（下一 outer 生效）；e 深改 **e**（下一 outer 生效）。
- 改动只落**每 run 私有 code 基线（code_root）**，真实仓库只读；信任锚永不可改。
- 应用前**编译/导入校验**，失败回滚；每代/每 outer 可追溯到 **code commit**。

## 进程模型
- **父 driver**（`repo_root`）：物化 code_root、起 worker、应用自改 patch、写 ckpt/commit、回滚。
- **子 outer worker**（`code_root`，`PYTHONPATH=code_root`）：执行**一个 outer**；`import gan.*` 来自 code_root。
- **孙 task**（worker 内 `domains.harness`）：在 `work/<genid>/repo`（允许清单最小副本）运行，标签在副本外。

## 运行流程（一个 outer）
1. 父 `materialize(repo_root, code_root)`（复制代码树、冻结只读、`git init`+commit）；resume 时 checkout 到 ckpt 的 code_commit。
2. 父起 worker（每 outer 一个子进程，timeout）。
3. worker：以当前 commit 构造 `Planner/Evaluator` → `_refresh_roles(N)` → inner 循环 → `self_improve` → 写 `ckpt/outer_<N>.json` 与 `ckpt/self_improve/*.patch`。
4. inner：
   - `planner.plan`：浅层改 design；深改 t 时经 gate+`edit_source` 改 workspace → patch → `apply_patch(code_root)`（t 基线）→ validate → commit；`Node.meta["task_code_commit"]`。
   - `task_runner`：`prepare_run_dir(code_root)` 最小副本（含最新 t 代码）+ `assemble_task_env(code_root)`；harness 子进程；证据落 `runs/<genid>`。
   - `evaluator.evaluate`。
   - `trajectory.collect` / `eval.json` / `scores` / 删 `work/<genid>` / `ckpt(inner)`。
5. 父读自改 patch → `apply_self_patch(code_root, role, patch)`（校验路径+编译）→ commit；失败 checkout 回滚并记事件。
6. 下一个 outer 的 worker 从新 commit 导入角色代码 → 自改生效。

## 隔离（可见 / 可修改）
四层叠加：**工具面**（只装配允许的工具；p/e 无 bash；读写工具限根于 workspace src）＋**授予面**
（按角色 allowlist + 物理为空按需复制 + 防递归/上限）＋**冻结面**（code_root 冻结只读 + patch
目标路径二次校验 + `validate_python`）＋**运行面**（每 outer 子进程；task 最小副本；标签在副本外）。
详见记录：`V4.14`（沙箱暂缓）与 `AGENTS.md` 的 "Access boundary"。

## 组件
- `gan/framework/code_repo.py`（冻结）：`materialize/apply_patch/apply_self_patch/validate_python/commit/checkout/current_commit/changed_files`。
- `gan/outer_worker.py`（glue）、`gan/driver.py`（glue）。
- `gan/framework/paths.py`：`code_root`、`code_manifest`。
- `gan/framework/loop.py`：抽 `run_outer(outer)`（`run()` 保留用于离线/烟雾）。
- `gan/tools/assembly.py` + `gan/registries/loader.py`：支持 `code_root=`。
- `gan/framework/task_execution.py`：`prepare_run_dir/assemble_task_env(..., code_root=)`；`task_runner(code_root=)`。
- `gan/build.py` / `scripts/run_gan.py`：`--code-repo` / `--in-process`；ckpt code 层（`code_commit`/`role_commits`）。

## 分阶段
- **A** CodeRepo 基础设施（`code_repo.py` + `paths`；离线测试）。
- **B** task 代码取自 code_root（p 改 t 生效）。
- **C** 单 outer 抽取 + worker + driver + 自改 apply/回滚（新进程重导入）。
- **D** ckpt code 层 / manifest / resume。
- **E** 小项：`unregister_component`、轮转、共享层归位、`e` 可见性确认。

## 决策（已确认）与实现状态
- 范围：**A+B+C 一次做完**。默认 **`--code-repo` 开**（`--in-process` 保留旧路径）。
- 双基线：**同仓按路径**（t 文件与 role 文件路径不重叠）。
- 共享层归位（`models`/`loader` → `utils/`）：**本轮不做**。

**已实现（离线验证）**
- A：`gan/framework/code_repo.py`（materialize/apply/validate/commit/checkout/changed_files）；
  `paths.code_root/code_manifest`；`test_code_repo`（含"新进程导入已改角色代码"）。
- B：`load_registry_for_role(registry_dir, components_dir)`、`assemble_tools_dir(code_root=)`、
  `Role(code_root=)`、`prepare_run_dir/assemble_task_env(code_root=)`、`task_runner(code_root=)`：
  p 的 t patch 施加到 code_root 并 commit，task 运行从 code_root 取代码；`broker.repo_root=code_root`
  （授予/diff 针对当前基线，标签仍从 `repo_root` 经 `GAN_DATASET_ROOT`）；`loop._resync_workspace`。 **[已变]** 标签改为仅经 CLI `--dataset_root` 传入父 harness，不再经 `GAN_DATASET_ROOT`。
- C：`GanLoop.run(only_outer=)`；`gan/outer_worker.py`（单 outer、`cwd/PYTHONPATH=code_root`）；
  `gan/driver.py`（每 outer 一个子进程 + `apply_self_patches` 白名单/编译校验/回滚）；
  `run_gan.py` 默认走 driver。`test_self_patches_apply`、`test_run_only_outer`。
- 默认开：`build_gan_loop(code_repo=True)`（`_ensure_code_root` 幂等，worker 复用已 materialize 的树）。

**Phase E（小项，已实现，离线验证）**
- `unregister_component`：`gan/tools/deep/unregister_component.py` +
  `code_repo.unregister_component`（白名单校验 registry+module、删条目+删源文件、commit）；
  `test_unregister_component`。
- 文件轮转：`utils/trajectory_log.append(max_bytes=)` + `read_all`（分片 `path.N`，读时合并）；
  `trajectory.collect` 用 `read_all` 处理已轮转的 `chat_history_*`；events 也经同一写入器。
  `test_rotation`。

**仍待**：共享层归位（本轮不做）、`e` 可见性最终确认（当前只读可见）、
"自改 patch 的 import 级校验"（当前 `py_compile`；可选加空载 import/smoke）。

---

# v4.18 task_brief：给 p/t/e 传"任务简述"（框架注入，暂不进化，标记可解冻）

**问题**：实测 V2 中 task agent 输出整篇综述而非 `accept/reject` → paper_review 得分恒 0。
根因：p/t/e 都**没有收到任何任务描述**——t 只有默认 prompt `"You are an agent."` + 原始输入
（`task_agent.py`）；p 只有 feedback/issues/config（`gan/roles/planner.py`）；e 只有
`run_summary`（含 `contract_ok` 等机器事实，但无人读可读的任务语义）（`gan/roles/evaluator.py`）。
`output_contract`（`allowed_labels` 等）只被框架用于 `compute_report_summary`，从未传给角色。

**信息分层（只传第 1 层）**
1. **任务简述**（传）：语义 + 答案接口（如"论文评审，最终只答 accept 或 reject"）。
2. **判据内部**（不传）：`case_insensitive`/`strip`/精确匹配/无部分分等硬判机制。
3. **答案/分数**（blind 期不传）：ground truth、`overall_accuracy`。

**实现**
- `gan/framework/domains.yaml`：每域新增 `task_brief`（`default: null`）。本轮仅
  `paper_review`、`polyglot`、`imo_grading`、`imo_proof`、`imo_proof_grading`。
  **框架注入、暂不进入可进化 design；标注"可解冻"（未来独立研究轴）。**
- `task_execution.assemble_task_env(..., task_brief=)` → env `GAN_TASK_BRIEF`；
  `task_runner` 传 `self.task_brief`（来自 `resolve_domain`）。
- `task_agent.py`：读取 `GAN_TASK_BRIEF`，置于 design prompt 之后、`Task input` 之前（非 instruction 首行）。
- `GanLoop`：从 `domains[0]` 解析 `task_brief`；`planner.plan(task_brief=)` 与
  `evaluator.evaluate(task_brief=)` 注入到各自 instruction。
- **blind 卫生**：`evaluate` 的 blind `run_summary` 移除 `score` 与
  `report_summary.{overall_accuracy,random_guess_accuracy}`（`meta` 视图同样剔除），
  分数只在 reveal 经 `benchmark_score` 给。
- 离线验证：`test_task_brief`（brief 解析/注入 + 不泄漏判据）。

**仍待**：`contract_ok`/`label_distribution` 是否也在 blind 期隐藏（当前保留，属格式事实）；
其余域是否补 brief（本轮仅 3 类）。

---

# v4.19 实测观察（V5）与待决项

**1. planner 自改进"看不见自身 trajectory"（已核实）**
- `planner/evaluator.self_improve` 的 instruction 只含静态文本 + `Recent outcomes`
  （`loop.py`：`recent_plan={"advantages","task_tree_size"}`，
  `recent_eval={"digests","last_feedback"}`）。
- `trajectory_file=` 只是**输出 sink**；全仓没有任何代码把角色的 trajectory 读回 prompt
  （`git grep trajectory.read/read_all` 仅命中写入/路径）。`read_trajectory` 只读 task 的
  parent 代，不含角色自身历史。
- 结论：**"planner 能看见自身 trajectory"当前不成立**。若要让自改进基于自身历史，
  需显式把上一/近几次 `trajectory/outer_<O>/planner.jsonl`（或 digest）注入 self_improve。
  在此之前，`task_brief` 传入 self_improve 仍是有用的改进目标来源（待决）。

**2. "glob 当路径"（已修：①+②）**
- 原因：`list_editable` 返回**白名单 glob**；模型原样请求；`grant` 把 glob 当具体路径 →
  `is_allowed` 放行但 `os.path.exists` 为假 → 记 `missing`。
- **① grant 支持 glob 展开**：`gan/framework/access.py` 检测 `*?[` 时用
  `glob.glob(repo_root/rel, recursive=True)` 展开为具体文件，逐个再过白名单与防递归/上限。
- **② `list_editable` 返回具体文件**：新增 `frozen.expand_roots(root, roots, cap)`，
  `list_editable` 返回 `read_files`/`write_files`（具体存在的文件，非 glob）。
- 离线验证：`test_glob_grant`、`test_list_editable`。

**3. 格式问题被判 `cannot_run`（记入文档，暂不改）**
- V4 实测：task 输出违反 `accept/reject` 契约（格式问题），evaluator 触发
  `eval_hard_failure` → `penalties.cannot_run=1`。语义上"格式违规 ≠ 不能运行"。
- 决定：**记入文档，暂时不改**（属 evaluator 判据/评估点语义，后续单独处理）。

**4. /tmp 产物易失**
- WSL 空闲后 `/tmp`（tmpfs）会被清空；后续 live 检查写到仓库内 `outputs/live_checks/`
  （gitignored）以持久化。

---

# v4.20 trajectory 可见性与 p/e 轨迹统一管理（对齐实现）

**问题**（内层检查结论）
- `trajectory.collect` 在 `add_node` **之后**，而 `evaluate` 在之前 → 当前代 task 轨迹在评估时**尚不存在**。
- `read_trajectory` 用 `visible_genids(tree, node_id)` 解析，而 plan/evaluate 时新节点**尚未入树** **[已变]** `visible_genids` 已删除，`read_trajectory` 改为读 `AccessContext.trajectory_genids`。
  → 恒返回空；即便入树也只给父代。
- p/e 轨迹由角色**直接写盘**（`trajectory_log.append`），**不脱敏、无 reader、无索引**，与 task 不一致。

**实现**
1. **提前归档**：`loop` 在 `task_runner` 之后、`evaluate` 之前调用 `trajectory.collect`；
   代末仍清理 `work/<genid>`。
2. **显式可见集**：`AccessContext.trajectory_genids`（`context.py`）+ `set_access_context(..., trajectory_genids=)`；
   `read_trajectory` 直接用它（不再查树）。`loop` 传入：
   - **planner.plan** → `[parent.genid]`（它要改进的节点）；
   - **evaluator.evaluate** → `[当前 genid] + [parent.genid]`（默认读当前代）；
   - **self_improve** → 本 outer 全部 genid + 最后一个的直接父代（已确认包含直接父代）。
3. **p/e 轨迹脱敏**：`Role.run` 结束调用 `trajectory.redact_file(path)`（冻结策略），
   与 task 一致（分片合并后重写）。
4. **统一读取**：`trajectory._render/read/read_session/outer_session_index`；
   新增 `gan/tools/work/common/read_session_trajectory.py`（读自身本 outer 会话，默认全部、限长）。
5. **self_improve 注入索引**：instruction 增加"本 outer 会话索引"（genid+bytes），
   指向 `read_session_trajectory`（默认不注入全文）。
6. **max_tool_calls**：`planner/evaluator.self_improve` 由 8 → 30（可配
   `loop.yaml: loop.self_improve_max_tool_calls`）；其余位置均默认 40，无需改。

**离线验证**：`test_traj_visibility`（显式可见集/越界拒绝/角色会话脱敏/会话索引/`read_session_trajectory`）；
全量 `py_compile`、`smoke_p1`、`smoke_gan`、`test_frozen`、`build_gan_loop` 全绿。

**仍待**：默认投递取"索引 + 一行摘要"还是"注入当前/父代脱敏摘要正文"（当前=索引+按需全文）；
`index.jsonl` 落盘（可选）。

---

# v4.21 各 domain 可通性分析（除 paper_review 外目前均不可直接跑通）

| domain | harness | report | 数据集 | brief / contract | 判定 |
|---|---|---|---|---|---|
| **paper_review** | ✅ | ✅ | ✅ | ✅ / ✅(label) | 可通（已实测） |
| search_arena | ✅ | ✅ | ❌ 仅 `dataset_filtered_100_ids.json` | ❌ / ❌(freeform) | 不通 |
| imo_grading | ✅ | ✅ | ❌ `domains/imo/` 无 csv | ✅ / ✅(label) | 不通（最接近） |
| imo_proof | ✅ | ⚠️ 需 `--model`（GAN report 不传） | ❌；`proofgrader_repo` 不存在 | ✅ / ❌ | 不通 |
| imo_proof_grading | ✅（需 `--proofs_dname`，GAN 不传） | ❌ report choices 不含 | ❌ | ✅ / ✅ | 不通 |
| polyglot | ❌ harness choices 不含（独立 docker harness） | — | 有 metadata | ✅ / ❌ | 不通 |
| balrog_* | ✅（hydra/依赖，可能 GPU） | ✅ | env 型 | ❌ / ❌ | 理论勉强；无 brief；GAN 未把分 /100 |
| genesis_* | ✅（需 GPU/Genesis） | ✅ | env 型 | ❌ / ❌ | 不通 |

**跨域阻断点**
1. 数据集缺失（search_arena / imo_* 的 csv 需先生成）。
2. `task_execution.run_harness_and_report` 调 `domains.report` **不传 `--model`**（imo_proof 必需）。
3. `domains.report` choices 不含 `imo_proof_grading`；harness 对 imo_proof_grading 需 `--proofs_dname`。
4. polyglot 不在 `domains.harness`（GAN `DomainTaskRunner` 无法驱动）。
5. task_brief 仅覆盖 paper_review/polyglot/imo_*；search_arena/balrog/genesis 无 brief、无 contract。
6. balrog 分数在 DGM-H 里 `/100` 归一，GAN `extract_score` 取原值（量纲不一致）。
7. balrog/genesis 依赖/GPU 与最小运行副本资产。

**最小工作（按性价比）**
1. imo_grading：生成 `domains/imo/gradingbench_<subset>.csv` 即可尝试。
2. search_arena：生成 `dataset_<subset>.csv` + 补 brief/contract（winner 词表）。
3. imo_proof(_grading)：report 传 `--model`、接 `--proofs_dname`、生成数据集、装 `proofgrader_repo`、report 增域。
4. polyglot：GAN 增专用 runner 或 `domains.harness` 增 polyglot 分支。
5. balrog/genesis：补 brief、统一分数口径、确认依赖/GPU 与副本资产。

---

# v4.22 code_root 裁剪 + self_improve 直接提交（选项 B）

- **code_root 裁剪**（`gan/framework/code_repo.py`）：`_COPY_TOP` 缩为
  `["gan","agent","utils","domains","task_agent.py"]`；`_COPY_FILES` 清空（不再拷
  `README.md/requirements*.txt/scripts/`，运行既不需要也不修改）。`gan/driver.py` 给
  worker 设 `PYTHONDONTWRITEBYTECODE=1`，避免在 code_root 生成 `__pycache__`。
- **self_improve 直接提交（选项 B）**：取消单独的 patch 存储
  （删 `ckpt/self_improve/*.patch`、`paths.self_improve_patch`、driver `apply_self_patches`）。
  `loop`（worker）在 outer 末对 `self_improve` 返回的 patch 直接
  `code_repo.apply_self_patch`（白名单+编译校验），失败 `checkout` 回滚；记事件
  `self_improve_commit{role,outer,commit}` / `self_improve_apply_failed{role,outer,error}`。
  **持久记录 = code_root 的 git commit**（人类可读性由 `git log/show`）。
- `GanLoop(code_root=)`；`build` 传入。离线验证：`test_self_patch_commit`（worker 直接提交）、
  `test_dual_loop`（无 patch 目录）、全量 smokes 绿。

---

# v4.23 预算耗尽的回执 / workspace 暂存（决策）

## max_tool_calls 触顶修复（已实现）
- `agent/llm_withtools.py`：触顶时**不再直接 break 返回半截**，而是追加**一次"无工具收尾回合"**
  （提示"Do NOT call any tool, provide your final answer/summary"）；`return_info=True` 时返回
  `(history, {"truncated", "tool_calls"})`（默认返回不变，向后兼容）。
- `gan/roles/base_role.py:Role.run` 存 `self.last_run_info`；离线 `test_tool_budget_grace` 通过。

## 回执（receipt）设计（待实现）
- **结构化、代码生成**（不使用 LLM digest）：本轮"成功的浅层设计改动"与"被拒的代码补丁"
  **分开标注**；含 `design.applied/diff`、`code_patch.applied/files/commit/rejected_reason`、
  `budget{exhausted,kind,tool_calls,attempts}`、`grants`、`trajectory_refs`、`next_hint`。
- **留痕**：`runs/<genid>/patch_receipt.json` + `runs/<genid>/patch_proposed.diff`（被拒补丁原文）
  + events；失败轨迹已脱敏落盘可回看。
- **注入**：下一轮 `planner.plan(receipt=)` / `evaluator.evaluate(run_summary.receipt=)` /
  `self_improve(recent.receipt=)`；`loop._pending_receipts` 随 checkpoint 持久。
- 耗尽策略：**方案 3**（补丁非法→回滚+用 prev 运行+记录/回执，节点仍可作父代）；
  硬失败（harness 挂/无测量）用**方案 2**（`_archive_invalid`，不作父代）。

## workspace 暂存 / 跨 outer 恢复（决策：方案 A）
- **背景**：工作区键是角色实例（`outer_<O>`），每 outer 开始 `clear_workspace`；因此
  **跨 outer 会丢"未提交的源码编辑"**（浅层设计已落 config、提议补丁与轨迹可留痕）。
  最需要继续的场景是 **self_improve**（在 outer 末运行，唯一继续点是下一 outer）。
- **方案 A（采用，先不做暂存）**：靠"结构化回执 + `patch_proposed.diff` + 轨迹"让下一轮**快速重做**。
  零额外机制、隔离清晰；代价是大型深改跨 outer 需重做。
- **方案 B（暂缓）**：定向携带——outer 末若该角色有 pending 编辑，把 `src` 拷到
  `pending/<role>/`，下一 outer 恢复。**难点=基线漂移**：code_root 可能在两 outer 间变化；
  恢复策略应为"仅恢复仍可编辑且 code_root 未变的文件，其余作为草稿提示重做"，并设过期/容量上限。
- **方案 C（否决）**：角色级持久工作区（不随 outer 清空）→ 破坏"每 outer 刷新/隔离"，残留累积。

---

# v4.24 健壮性（字段无关）+ 统一提交校验 + unregister 不再即时提交

**L1 注册表契约与容错读取**（`gan/registries/loader.py`）
- 组件契约为通用校验：必需字段 `name/kind/module`（kind ∈ KINDS），`module` 文件须存在；
  非 dict/缺字段/未知 kind/文件缺失/重复（按条目）均标 **invalid + reason**。
- `parse_registry_file` 承认"整文件不可解析"（非法 JSON / 非对象 / components 非列表）。
- `ComponentRegistry`：`is_valid/reason/module_path(.get)/invalid()`；`load_registry_for_role(tolerant=True)`
  **不抛**。

**L2/L3/L4/L7 使用点严格、不崩**
- `select_component`：选中前要求 `is_valid`，否则返回清晰 Error。
- `assembly.selected_module_paths`：`module_path` 为 None → 跳过（无效/缺失组件不装配）。
- `agent/tools/__init__.py:load_tools`（底座）：模块 import 失败或缺 `tool_info/tool_function` →
  **log+skip**（不再 raise）。
- `loop._archive_invalid` / evaluator_failed 分支：清理 `work/<genid>`。

**统一提交校验（覆盖 self 与 p→t 两条路径）**（`gan/framework/code_repo.py`）
- `registry_report(code_root)`：invalid 键 `(file,kind,name)` + `unparseable(file)` 集合。
- `_registry_worsened(before, after, strict=True)`：新增 unparseable 或新增 invalid 即"变差"；
  **before 不可解析取严**（须变为"可解析且无非法"才放行）。
- `check_patch(code_root, patch)`：apply→编译→registry 比较→**始终回滚**（供会话内重试 dry-run）。
- `apply_code_patch(code_root, role, patch, msg)`：白名单→before→apply→编译→registry→（失败回滚）
  否则 commit；失败抛 `PatchRejected(reason)`。
- `apply_self_patch` = 其薄封装；`task_runner` 的 t 补丁改走 `apply_code_patch(...,"planner",...)`，
  被拒 → `Node.meta["task_patch_rejected"]`、用 prev 运行（**不 invalid**）。
- `changed_files` 支持删除（`+++ /dev/null` → 取 `--- a/` 路径）；`gan/patch.build_patch_from_workspace`
  支持删除（工作区缺失=删除 diff）。

**`unregister_component` 不再即时提交**
- 改为**工作区操作**：把 registry（及 module）grant 进工作区 → 在 workspace 删除条目/文件 →
  记入该会话补丁，随统一校验一起提交；不再 mid-session `commit`。

**离线验证**：新增/更新 `test_registry_tolerance`、`test_load_tools_tolerant`、
`test_unregister_component`（工作区化）、`test_code_repo`（`PatchRejected`）；全量 smokes 绿。

**仍待**：会话内**原地重试（K）** + **回执（receipt）**构建/注入 + 耗尽策略（选项 3）接线；
可选 `runs/<gid>/invalid.json`。

---

# v4.25 会话内原地重试 + 回合回执（receipt）

**原地重试（K=2，默认）**
- `planner.plan` 与 `planner/evaluator.self_improve`：`run → build patch → code_repo.check_patch(dry-run)`
  → 非法则**同一会话续聊**（带拒绝原因）重试；**无进展**（补丁 hash 不变）或到 K 即停。
- 返回 `patch`（合法才非空）+ `patch_proposed` + `patch_rejection` + `attempts` + `truncated`
  + `budget_exhausted`（预算来自 `self.last_run_info`，即 max_tool_calls 修复后的标志）。
- 配置：`loop.yaml` 增 `patch_retry_k: 2`、`plan_max_tool_calls: 40`。

**回合回执（`gan/framework/receipt.py`，确定性/无 LLM）**
- `build_receipt(...)`：`design{applied, prompt_changed, skills_added/removed, eval_points_*, ops}`
  + `code_patch{proposed, applied, commit, rejected_reason}` + `budget{exhausted,kind,attempts,truncated}`
  + `grants` + `trajectory_refs` + `next_hint`（模板化修复建议）。
- `render_receipt(...)`：紧凑文本，注入 prompt；无内容返回空。
- 落盘：task 回合 `runs/<genid>/patch_receipt.json`（被拒另存 `patch_proposed.diff`）；
  自改回合 `runs/self_outer_<N>_<role>/patch_receipt.json`；并记 `events`（`receipt`/`self_receipt`）。
- 注入：`loop._last_receipt` 传给下一轮 `planner.plan(receipt=)` 与 `evaluator.evaluate(run_summary.receipt=)`；
  `loop._last_self_receipt` 传给下一 outer 的 `self_improve(recent.receipt=)`。
- **成功与失败分开标注**：`design.applied`（浅层改动已生效、子代已继承）与
  `code_patch.applied/rejected_reason`（代码是否落基线）互不覆盖。

**耗尽策略**：补丁被拒（重试耗尽）→ 回滚、用 `prev` 继续、节点仍可作父代（`Node.meta["task_patch_rejected"]`），
回执前传；硬失败仍走 `_archive_invalid`。**不使用 LLM digest**；**workspace 暂存（方案 B）暂缓**。

**离线验证**：`test_check_patch`（dry-run 拒绝/接受、始终回滚）、`test_receipt`（结构+渲染）；
全量 smokes 绿。

---

# v4.26 invalid 代的最小记录

- `loop._archive_invalid` / `evaluator_failed` 分支：除归档无效节点、清理 `work/<genid>` 外，
  写 `runs/<genid>/invalid.json`（`{genid, reason, detail, outer, inner}`），
  使"`runs/` 目录不连续"可解释/可审计。
- 离线验证：`test_invalid_json`。
