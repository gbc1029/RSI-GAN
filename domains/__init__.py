"""HyperAgents domains package.

Empty package marker. It is required at repo/copy root so the bwrap
sandbox (``domains/harness.py``) can bind-mount it read-only into the
TaskAgent sandbox without a missing-source failure.
"""
